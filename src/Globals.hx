
package ;

/*
Globally accessible settings, etc..

Try to avoid ruining encapsulation!
(This is only for settings and occasional convenience functions which
don't require a full class to themselves - be responsible!)

 */
class Globals {
	public static var USE_MOVEMENT_SPEED:Bool = false;
}